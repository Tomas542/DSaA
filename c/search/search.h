#include <stddef.h>

int linear_search(int array[], size_t array_size, int target);

int binary_search(int array[], size_t array_size, int target);

int fibbonaci_search(int array[], size_t array_size, int target);